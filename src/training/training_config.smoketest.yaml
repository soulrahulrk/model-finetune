# Local smoke-test config -- NOT for real training. Swaps in a tiny model + tiny sequence
# length so the QLoRA pipeline (SFTConfig construction, eos_token handling, trainer wiring)
# can be validated end to end in seconds/minutes on a 6GB laptop GPU instead of the full
# Qwen3-8B run. Once this passes cleanly, the exact same train_unsloth.py code path is trusted
# to work against training_config.yaml (the real config) on Colab.

model:
  base_model: "Qwen/Qwen3-0.6B"
  max_seq_length: 512
  load_in_4bit: true
  dtype: null
  enable_thinking: false

lora:
  r: 8
  lora_alpha: 8
  lora_dropout: 0.05
  bias: "none"
  target_modules:
    - q_proj
    - k_proj
    - v_proj
    - o_proj
    - gate_proj
    - up_proj
    - down_proj
  use_gradient_checkpointing: "unsloth"
  random_state: 42

data:
  train_file: "../../data/processed/train.jsonl"
  val_file: "../../data/processed/val.jsonl"
  packing: false
  train_on_responses_only: true
  instruction_part: "<|im_start|>user\n"
  response_part: "<|im_start|>assistant\n"

training:
  output_dir: "./outputs/smoketest-qlora"
  num_train_epochs: 1
  per_device_train_batch_size: 2
  per_device_eval_batch_size: 2
  gradient_accumulation_steps: 1
  learning_rate: 2.0e-4
  lr_scheduler_type: "cosine"
  warmup_ratio: 0.05
  optim: "adamw_8bit"
  weight_decay: 0.01
  max_grad_norm: 0.3
  bf16: true
  fp16: false
  logging_steps: 1
  eval_strategy: "epoch"
  save_strategy: "epoch"
  save_total_limit: 1
  load_best_model_at_end: true
  metric_for_best_model: "eval_loss"
  greater_is_better: false
  early_stopping_patience: 2
  seed: 42
  report_to: "none"

export:
  merged_fp16_dir: "./outputs/smoketest-merged-fp16"
  adapter_only_dir: "./outputs/smoketest-adapter"
  gguf_dir: "./outputs/smoketest-gguf"
  gguf_quant_methods:
    - "q4_k_m"
